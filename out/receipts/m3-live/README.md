# M3 live run receipt — Darwinbox "Start and Scale AI in HR" (2026-09-16, IST evening)

Copy of `out/verify-w3/m3/` from one `repurpose.py --clips --images` run on the default live lane
(text model `nvidia/nemotron-3-super-120b-a12b:free` via OpenRouter; image model attempted `google/gemini-2.5-flash-image`).

Included: `blog.md`, `youtube.md`, `infographic.md`, `social.md`, `extraction.json`, `grounding_report.json`,
`manifest.json`, `receipts/` (per-call LLM log, raw completions, clip + image receipts), `clips/*.srt`, `clips/*-thumb.png`.

Omitted from git on purpose: the six MP4 clips (14 MB; regenerated by `make_clips.py` from the source media, and
published to the shared drive by the `record` phase) and the per-line caption PNG frames (ffmpeg intermediates).

Images: 0 of 3 generated — every attempt returned HTTP 402 (no purchased credits on the key). Recorded in
`receipts/m3_images.json` and `manifest.json.notes`; no placeholder was written in their place.
