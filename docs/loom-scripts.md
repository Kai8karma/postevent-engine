# Loom Walkthrough Scripts — superseded (v1)

Five 90-second narration scripts written for the v1 submission (rejected
2026-09-12 for not following the case study) — no video was ever recorded
against them, and the modules/UI they narrate (the old single-script M4
dashboard builder, the old control-room lanes table) no longer match v2's
code.

v2 has not scripted or recorded a walkthrough. If one is needed, write it
fresh against the current build:

- `docs/module-api.md` for the phase-by-phase contract each module exposes.
- `modules/*/README.md` for what each module actually produces and shows.
- `_internal/REVISION-PLAN-2026-09-12.md` for the current build state —
  what's live, what's still gated on a key or a scope.
